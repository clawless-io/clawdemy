---
title: "References: How transformers learn language understanding: BERT, MLM, and NSP"
description: "Annotated links for readers who want to go deeper on BERT, MLM, NSP, the encoder-only architectural choice, and the train-then-fine-tune workflow that defined a generation of NLP."
artifact: references
sidebar:
  order: 6
  label: References
tableOfContents: false
---

## Source material

```
Source material:
• Stanford CME 295: Transformers & Large Language Models, Autumn 2025
  Instructor: Afshine Amidi & Shervine Amidi, Stanford University
  Course site: https://cme295.stanford.edu/
  Cheatsheet: https://cme295.stanford.edu/cheatsheet/
  Source lecture (Lecture 2, Transformer-based models & tricks):
    https://www.youtube.com/watch?v=yT84Y5zCnaA
  License (lecture videos): as published on Stanford's public YouTube channel
  License (Amidi cheatsheets): MIT
This lesson adapts the BERT section of Stanford CME 295 Lecture 2
(~4090s-5990s, the largest single section in the lecture). The next
lesson covers BERT derivatives: DistilBERT (compression via
distillation) and RoBERTa (training improvements over the original
BERT recipe). Clawdemy provides original notes, summaries, and quizzes
derived from this material for educational purposes. All rights to the
original lectures remain with Stanford and the instructors.
```

## Going deeper

A short list, chosen for durability.

- **["BERT: Pre-training of Deep Bidirectional Transformers for Language Understanding"](https://arxiv.org/abs/1810.04805)**, Devlin et al., 2019. The BERT paper. The lecturer's "around 170k citations" claim refers to this paper. Section 3 covers the architecture and pretraining objectives in detail; section 4 covers fine-tuning. The paper is famously readable for a foundational ML paper.

- **["Deep contextualized word representations"](https://arxiv.org/abs/1802.05365)**, Peters et al., 2018. The ELMo paper the lecturer mentions. Worth reading for the bidirectional-LSTM precursor approach to bidirectional representations and the contrast with what made BERT scale better.

- **["Attention Is All You Need"](https://arxiv.org/abs/1706.03762)**, Vaswani et al., 2017. The original transformer paper. BERT is the encoder of this architecture, applied with a different pretraining objective. Read section 3 for the encoder mechanics if you have not already.

- **[Hugging Face Transformers documentation for BERT](https://huggingface.co/docs/transformers/model_doc/bert)**. The de facto reference for using BERT in practice. Covers tokenizers, model variants (cased, uncased, multilingual), fine-tuning examples, and loadable pre-trained checkpoints.

- **["Subword Tokenization and the Word-Pieces Algorithm"](https://research.google/pubs/japanese-and-korean-voice-search/)**, Schuster & Nakajima, 2012. The original WordPiece paper (Google Research). For readers who want to dig into the tokenizer BERT uses; also relevant background for our tokens lesson in the Lecture 1 adaptation.

- **[Stanford CME 295 cheatsheet](https://cme295.stanford.edu/cheatsheet/)** by the Amidi twins. MIT-licensed. Single-page reference for the same material in their dense visual style.

## Adjacent topics

Topics that build on or sit beside this one.

- **The BERT family.** BERT seeded a large family of variants and improvements. The next lesson covers two important ones (DistilBERT, RoBERTa). Others worth knowing about by name: ALBERT (parameter-efficient via cross-layer weight sharing), ELECTRA (replaces MLM with replaced-token-detection for sample efficiency), DeBERTa (disentangled attention with separate position encoding), BERTweet / BioBERT / SciBERT (domain-adapted variants).

- **Cross-lingual BERT variants.** mBERT (multilingual BERT, trained on 104 languages) and XLM / XLM-R (cross-lingual language models) extend the BERT recipe to multilingual settings. Loosely parallels the mT5 line in the encoder-decoder family from L2.4.

- **Sentence embeddings.** BERT's CLS embedding is sometimes used directly as a sentence embedding for similarity tasks, but the paper Sentence-BERT (Reimers & Gurevych, 2019) showed that vanilla BERT CLS embeddings are surprisingly weak for similarity; a small fine-tuning step on a similarity-shaped task produces dramatically better sentence embeddings. Worth reading if you build retrieval or semantic-search systems.

- **The "post-BERT era" and decoder-only's rise.** Encoder-only models (BERT and family) dominated NLP from 2018 through about 2020. Then GPT-3-style decoder-only models started to dominate the discourse, in part because of in-context learning capabilities the encoder-only models didn't offer. Both branches still ship; the use cases differ. Background context for our Lecture 5 lesson on fine-tuning + RLHF.

- **Where to go next.** The next lesson in this lecture covers BERT derivatives: **DistilBERT** (compression via knowledge distillation) and **RoBERTa** (which dropped NSP and showed that training MLM longer on more data hurts nothing). That lesson closes the second arc of Lecture 2 and the lecture itself.

## Original sources

The primary papers, in chronological order.

- **["Attention Is All You Need"](https://arxiv.org/abs/1706.03762)**, Vaswani et al., 2017. The original transformer.

- **["Deep contextualized word representations"](https://arxiv.org/abs/1802.05365)**, Peters et al., 2018. ELMo.

- **["BERT: Pre-training of Deep Bidirectional Transformers for Language Understanding"](https://arxiv.org/abs/1810.04805)**, Devlin et al., 2019. BERT.

- **["Sentence-BERT: Sentence Embeddings using Siamese BERT-Networks"](https://arxiv.org/abs/1908.10084)**, Reimers & Gurevych, 2019. Sentence-BERT, for sentence-similarity fine-tuning of BERT.

## Community discussion

None selected for this lesson. BERT's place in NLP is well-established and the relevant discussion has consolidated into the academic literature plus the Hugging Face documentation cycle. Durable references will be added at a future quarterly review if any consolidate.
