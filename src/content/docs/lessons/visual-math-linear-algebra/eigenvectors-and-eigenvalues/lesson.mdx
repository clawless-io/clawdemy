---
title: "Lesson: The stubborn vectors, eigenvectors and eigenvalues"
description: "When a transformation moves the plane, most vectors get knocked off their own line. A few stubborn ones do not, they stay on their line and only get scaled. Those are eigenvectors, the scaling factor is the eigenvalue, and together they reveal what a transformation is really doing."
artifact: lesson
sidebar:
  order: 2
  label: Lesson
---

When a linear transformation moves the plane, watch what happens to a single vector. Most vectors get knocked off their own line: the arrow ends up pointing in a different direction than it started. But for a given transformation, a few special vectors are stubborn. They stay on the same line they started on, getting stretched longer, squished shorter, or flipped to point backward, but never rotated off their line.

Those stubborn vectors are called **eigenvectors**, and the factor by which each one gets scaled is its **eigenvalue**. They are the deepest idea in this track, because they reveal what a transformation is actually doing underneath the obfuscation of whatever basis you happened to write it in. This lesson finds them, both by eye and by formula, and closes the promise from the change-of-basis lesson: eigenvectors are the basis in which a transformation looks as simple as it possibly can.

## The definition

An **eigenvector** of a matrix `M` is a nonzero vector `v` that `M` merely scales, without knocking it off its line:

```
M · v = λ · v
```

The scalar `λ` (lambda) is the **eigenvalue**: the factor `v` gets scaled by. The equation says something strong. On the left, `M` does its full transformation to `v`. On the right, all that happened was multiplication by a single number. For an eigenvector, the entire matrix acts like a plain scalar. No rotation, no shear, just scaling.

The geometric way to hunt for them: imagine sweeping an input vector all the way around the unit circle and watching where the transformation sends it. For most input directions, the output points somewhere off the input's line. At a few special directions, the output snaps back onto the same line. Those directions are the eigenvectors, and how much the transformation stretched or flipped them is the eigenvalue.

{/* DIAGRAM PLACEHOLDER: an input vector swept around the unit circle with its transformed output shown alongside; at most positions the output is off the input's line, but at the eigenvector directions the output lands back on the same line */}

## Reading eigenvectors by eye

Some transformations let you spot the eigenvectors without any calculation.

**A pure stretch.** Take `M = [[2, 0], [0, 3]]`, which stretches the x-direction by 2 and the y-direction by 3. Look at `i-hat = [1, 0]`: the matrix sends it to `[2, 0]`, which is `2 · i-hat`, still on the x-axis. So `i-hat` is an eigenvector with eigenvalue 2. Likewise `j-hat = [0, 1]` goes to `[0, 3] = 3 · j-hat`, an eigenvector with eigenvalue 3. Here the standard basis is already the eigenvector basis, and, not coincidentally, the matrix is already diagonal: the eigenvalues sit right on the diagonal.

**A rotation.** Take the 90-degree rotation `R = [[0, -1], [1, 0]]`. Every vector gets spun a quarter turn, so no vector stays on its own line. A pure 2D rotation has no real eigenvectors at all. (It has eigenvalues in the complex numbers, which is how rotations are handled in more advanced settings, but that is beyond this lesson; in the real plane, there are simply none.)

**A shear.** Take `S = [[1, 1], [0, 1]]`. The vector `i-hat` stays put at `[1, 0]`, an eigenvector with eigenvalue 1. But `j-hat` gets sheared to `[1, 1]`, off its line, so it is not an eigenvector. The shear has only one eigenvector direction, the x-axis. This is a degenerate case worth seeing: a transformation need not have a full set of independent eigenvectors.

{/* DIAGRAM PLACEHOLDER: three panels: the stretch (i-hat and j-hat both stay on their axes), the rotation (every vector rotated off its line, no eigenvectors), and the shear (only the x-axis stays fixed) */}

## Finding eigenvalues by formula

By eye works for tidy matrices; for the rest, there is a method. Start from the definition and rearrange:

```
M · v = λ · v   ⟹   M · v - λ · v = 0   ⟹   (M - λI) · v = 0
```

where `I` is the identity matrix, so that `λI` is the matrix that scales everything by `λ`. We want a *nonzero* `v` that `(M - λI)` sends to the zero vector. From the inverses lesson, a matrix sends a nonzero vector to zero exactly when it collapses space, which happens exactly when its determinant is zero. So the eigenvalues are the values of `λ` that make

```
det(M - λI) = 0
```

This is the **characteristic equation**. Solve it for `λ`, then for each `λ` find the vectors `(M - λI)` crushes to zero, the null space from the inverses lesson, and those are the eigenvectors.

## A full worked example

Take `M = [[3, 1], [0, 2]]` and find everything.

First the eigenvalues. Subtract `λ` down the diagonal:

```
M - λI = [ 3-λ   1  ]
         [  0   2-λ ]
```

Its determinant is `(3-λ)(2-λ) - (1)(0) = (3-λ)(2-λ)`. Set it to zero: the solutions are `λ = 3` and `λ = 2`. Those are the two eigenvalues.

Now the eigenvector for each. For `λ = 3`, form `M - 3I = [[0, 1], [0, -1]]` and solve `(M - 3I) · [x, y] = 0`. Both rows say `y = 0`, with `x` free, so the eigenvectors are everything along `[1, 0]`. For `λ = 2`, form `M - 2I = [[1, 1], [0, 0]]` and solve: the top row says `x + y = 0`, so `y = -x`, giving the line through `[1, -1]`.

Check both against the definition:

```
M · [1, 0]  = [3, 0]   = 3 · [1, 0]    eigenvalue 3, confirmed
M · [1, -1] = [2, -2]  = 2 · [1, -1]   eigenvalue 2, confirmed
```

`M · [1, -1] = [3·1 + 1·(-1), 0·1 + 2·(-1)] = [2, -2]`, which is indeed `2 · [1, -1]`. Two eigenvalues, two eigenvector directions, all verified.

That matrix was upper-triangular, which made the determinant factor on sight. Here is one where it does not. Take `M = [[2, 1], [1, 2]]`. Then `M - λI = [[2-λ, 1], [1, 2-λ]]`, and its determinant is `(2-λ)(2-λ) - (1)(1) = (2-λ)^2 - 1`. Setting that to zero gives `(2-λ)^2 = 1`, so `2 - λ = 1` or `2 - λ = -1`, which means `λ = 1` or `λ = 3`. For `λ = 3`, `M - 3I = [[-1, 1], [1, -1]]` and the equation `-x + y = 0` gives `y = x`, the line through `[1, 1]`. For `λ = 1`, `M - I = [[1, 1], [1, 1]]` and `x + y = 0` gives the line through `[1, -1]`. Check: `M · [1, 1] = [3, 3] = 3 · [1, 1]` and `M · [1, -1] = [1, -1] = 1 · [1, -1]`. The characteristic equation handled a matrix whose eigenvectors were nowhere obvious by eye.

{/* DIAGRAM PLACEHOLDER: the two eigenvector lines for M=[[3,1],[0,2]] drawn through the origin (the x-axis for lambda=3, the line through [1,-1] for lambda=2), with vectors on each line stretched by their eigenvalue */}

## The payoff: the diagonal basis

This is what the change-of-basis lesson promised. When a 2x2 matrix has two independent eigenvectors, they form a basis, and in that basis the transformation becomes pure scaling along the axes, the simplest possible matrix: a diagonal one.

Build `P`, the matrix whose columns are the eigenvectors, and change basis with the sandwich from last lesson:

```
P = [ 1   1 ]        D = P^-1 · M · P
    [ 0  -1 ]
```

Run it through for our `M = [[3, 1], [0, 2]]`. The inverse of `P` (using the 2x2 shortcut from last lesson, with `det(P) = -1`) happens to be `P` itself: `P^-1 = [[1, 1], [0, -1]]`. Compute `M · P` first, by applying `M` to each column of `P`: `M · [1, 0] = [3, 0]` and `M · [1, -1] = [2, -2]`, so `M · P = [[3, 2], [0, -2]]`. Then `P^-1 · (M · P)`, applying `P^-1` to each of those columns, gives `[3, 0]` and `[0, 2]`. So:

```
D = P^-1 · M · P = [ 3  0 ]
                   [ 0  2 ]
```

a diagonal matrix with the eigenvalues sitting on the diagonal. In the eigenvector basis, the messy-looking `M = [[3, 1], [0, 2]]` is revealed as nothing more than "stretch by 3 along one eigenvector, by 2 along the other." The shear-like off-diagonal entry was an artifact of describing the transformation in the standard basis. Change to the basis the transformation itself prefers, and it turns transparent.

That is the deep reason eigenvectors matter: they are the natural coordinate system of a transformation. The eigenvalues are what it actually does, stripped of the arbitrary basis you first wrote it in.

## Why this matters when you use AI

Eigenvalue and eigenvector analysis is genuinely central across machine learning, more so than the cross product was.

**Principal Component Analysis** is the headline example. PCA finds the eigenvectors of a dataset's covariance matrix; those eigenvectors point along the directions of greatest variation in the data, the principal components, and the eigenvalues say how much variation each captures. Keeping the few eigenvectors with the largest eigenvalues compresses the data while preserving most of its spread. The change-of-basis and eigenvector ideas from these two lessons are exactly the machinery PCA runs on.

**Exploding and vanishing gradients,** the central difficulty in training deep and recurrent networks, is an eigenvalue story. When a signal passes repeatedly through a weight matrix, it gets scaled by the matrix's eigenvalues each time. Eigenvalues with magnitude above 1 make the signal blow up over many steps; eigenvalues below 1 make it decay to nothing. The whole stability of training depends on keeping those eigenvalue magnitudes near 1.

**Spectral methods in graph neural networks** use the eigenvectors of a graph's connectivity matrix to encode its structure, and classic algorithms like PageRank are, underneath, the search for a particular eigenvector. Once you know to look, eigenvalue problems are everywhere.

## Common pitfalls

**Forgetting eigenvectors are whole lines, not single arrows.** If `v` is an eigenvector, so is any nonzero scalar multiple of it: scaling `v` does not change the line it lies on. An eigenvector names a direction (a line through the origin), and people usually pick one representative vector on it.

**Expecting every matrix to have real eigenvectors.** Rotations in the real plane have none. Shears have only one direction instead of two. A full set of independent eigenvectors is common but not guaranteed.

**Misreading a zero eigenvalue.** An eigenvalue of zero is allowed and meaningful: it means the transformation crushes that eigenvector's direction to the origin, the collapse from the determinant and inverses lessons. Zero eigenvalue, not invertible.

**Mixing up eigenvalue and eigenvector.** The eigenvalue is the number (the scaling factor); the eigenvector is the direction that gets scaled. `M · v = λ · v`: `v` is the vector, `λ` is the number.

## What you should remember

- **An eigenvector is a nonzero vector the transformation only scales, not rotates: `M · v = λ · v`.** The eigenvalue `λ` is the scaling factor. Geometrically, eigenvectors are the directions that stay on their own line through the transformation; the eigenvalue is how much they stretch (or flip, if negative, or collapse, if zero).
- **Find eigenvalues by solving the characteristic equation `det(M - λI) = 0`,** then find each eigenvector as the null space of `M - λI`. This works because a nonzero vector maps to zero only when the matrix collapses, which is the zero-determinant condition from the inverses lesson.
- **In the eigenvector basis, the transformation is diagonal:** `D = P^-1 · M · P` has the eigenvalues on its diagonal, where `P`'s columns are the eigenvectors. This is the simplest a transformation can look, the basis it naturally prefers, and the reason eigenvectors are the workhorse behind PCA, gradient stability, and much more.

Most vectors are pushed around by a transformation; the eigenvectors hold their ground and only scale. Find them and you have found the transformation's true axes, the directions along which it is nothing more than stretching. The final lesson steps back from arrows entirely and asks what survives when "vector" means something that is not an arrow at all.
