---
title: 'Practice: AI-authored commits and PRs'
description: Self-check questions, drills, scenario reflections, and flashcards for L15. Model answers in the cheatsheet.
artifact: practice
sidebar:
  order: 3
  label: Practice
---

import Flashcard from '../../../../../components/Flashcard.astro';
import FlashcardSet from '../../../../../components/FlashcardSet.astro';

### Self-check questions

**Q1.** What's the difference between git's "author" and "committer" fields?

**Q2.** What's the standard convention for attributing AI contributions in a commit message?

**Q3.** Give three situations where you SHOULD include the Co-Authored-By trailer for an AI agent.

**Q4.** Give two situations where you should NOT include the trailer.

**Q5.** List four specific failure modes to look for when reviewing AI-authored code.

**Q6.** What's the accountability principle, in one sentence?

**Q7.** Should release notes call out AI contributions per-feature?

**Q8.** How would you search commit history for AI-authored commits?

### Trailer-writing drill

Write commit messages with appropriate trailers for each scenario:

1. You used an AI agent to refactor a 200-line function into three smaller functions. You reviewed the refactor carefully and confirmed it's behaviorally equivalent.
2. You used an AI agent to write a new database migration. You then hand-modified the migration based on feedback from your DBA.
3. You wrote a new feature entirely by hand. You asked the AI a clarifying question about a library's behavior at one point but didn't use any AI-generated code.
4. You used Claude Code (the specific tool) to add comprehensive test coverage to an existing module. The agent wrote ~250 lines of tests; you reviewed and added 30 more.

### Review-lens drill

For each AI-authored code snippet below, identify the failure mode (hallucinated API, plausible-but-wrong logic, trivial test assertion, confident-but-wrong docstring, etc.) and propose a fix:

**Snippet 1:**
```python
def get_user_by_email(email: str) -> User:
    return User.find_or_create_by_email(email)
```
(Assume your User model is a SQLAlchemy model with no `find_or_create_by_email` method.)

**Snippet 2:**
```python
def test_calculate_tax():
    result = calculate_tax(100, 0.10)
    assert result is not None
```

**Snippet 3:**
```python
def normalize_phone(phone: str) -> str:
    """Returns the phone number in E.164 format (+12025551234)."""
    return phone.replace("-", "").replace(" ", "")
```
(Note: removing dashes and spaces does NOT produce E.164 format.)

**Snippet 4:**
```python
def get_recent_orders(user_id: int, days: int = 7) -> list:
    cutoff = datetime.now() - timedelta(days=days)
    return Order.query.filter(Order.user_id == user_id).filter(Order.created_at > cutoff).all()
```
(Looks fine at first glance. What's missing?)

### PR description drill

You used an AI agent to add CSV export to your admin dashboard. The agent typed ~80% of the code (the export logic, the CSV formatter); you typed the rest (the route registration and the integration test). Write the PR description.

### Release-notes drill

Your team shipped 12 PRs this release. Five involved AI assistance. Write:
1. The user-facing release notes paragraph (3-5 lines)
2. The internal changelog paragraph (5-10 lines, more transparent)

### Scenario reflections

**Scenario A:** A new hire asks: "Why do we add Co-Authored-By trailers for AI? The AI isn't a person." Walk through your answer.

**Scenario B:** You're reviewing an AI-authored PR. The diff looks reasonable. Tests pass. You don't have time for a deep review and the PR author is asking for sign-off so they can ship before end-of-day. What do you do?

**Scenario C:** Your team has been using AI tools for 6 months. Time for a process retrospective. What metrics or signals would you look at?

**Scenario D:** A senior engineer pushes back: "All this attribution stuff is busywork. The code is fine; what matters is that it works." How do you respond?

### Flashcards

<FlashcardSet>

<Flashcard question="Git author field">
Who created the change. Stays with the commit forever (across cherry-pick, rebase). Recorded as `Name <email>`.
</Flashcard>

<Flashcard question="Git committer field">
Who applied the commit to the repo. Often same as author. Recorded as `Name <email>`.
</Flashcard>

<Flashcard question="Standard AI attribution mechanism">
`Co-Authored-By: <Agent Name> <noreply-email>` trailer at bottom of commit message
</Flashcard>

<Flashcard question="Trailer format">
Key: Value at the bottom of the commit message body. Examples: Co-Authored-By, Signed-off-by, Reviewed-by.
</Flashcard>

<Flashcard question="Generated with marker">
Optional richer convention: `Generated with [Claude Code](https://claude.com/claude-code)` line before the Co-Authored-By trailer
</Flashcard>

<Flashcard question="Include trailer when...">
AI agent typed the diff and human reviewed; commit was primarily-AI workflow; AI substantially contributed to commit message
</Flashcard>

<Flashcard question="Don't include trailer when...">
Human typed everything; AI was only consulted for research; AI provided typeahead but human structured the work
</Flashcard>

<Flashcard question="Hallucinated API">
AI calls a function that doesn't exist but sounds plausible. Catch by: verifying APIs against docs, integration tests against real services.
</Flashcard>

<Flashcard question="Plausible-but-wrong logic">
Code looks like it solves the problem but actually solves an adjacent or wrong problem. Catch by: walking through concrete inputs.
</Flashcard>

<Flashcard question="Trivial test assertion">
Test passes but asserts something meaningless (assert result is not None instead of result == expected). Catch by: reading assertions critically.
</Flashcard>

<Flashcard question="Confident-but-wrong docstring">
Sounds professional, describes wrong behavior. Catch by: reading docstrings against code, not just for plausibility.
</Flashcard>

<Flashcard question="Style mimicry over correctness">
Agent applies team style consistently but style is not the same as correctness. Catch by: not trusting 'looks right.'
</Flashcard>

<Flashcard question="Accountability principle">
The human who merges the work is responsible, regardless of who typed it.
</Flashcard>

<Flashcard question="Release notes treatment">
User-facing: light acknowledgment at release level. Internal: more transparency for process calibration.
</Flashcard>

<Flashcard question="Audit search">
`git log --grep='Co-Authored-By.*Claude'` finds AI-co-authored commits
</Flashcard>

</FlashcardSet>
