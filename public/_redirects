# Cloudflare Pages routing rules.
# Format: <source> <destination> <status>
# Docs: https://developers.cloudflare.com/pages/configuration/redirects/

# Pin the Google Search Console verification file at its .html path so
# Cloudflare Pages does not 308-redirect to the extension-stripped URL.
# Google requests the file at the exact .html path during verification;
# returning 200 there is the simplest path even though Google does follow
# 308s in practice.
/googlefe2ff7dd9c0f27bc.html /googlefe2ff7dd9c0f27bc.html 200
